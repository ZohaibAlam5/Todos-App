export default {
  plugins: {
    // If you are using Tailwind v4:
    '@tailwindcss/postcss': {},

    // OR, if the above fails, try the standard v3 plugin set:
    // 'tailwindcss': {},
    // 'autoprefixer': {},
  },
};