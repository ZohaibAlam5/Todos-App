apiVersion: v1
kind: Service
metadata:
  name: taskflow-mcp-server
  labels:
    app: taskflow
    component: mcp-server
spec:
  type: ClusterIP
  selector:
    app: taskflow
    component: mcp-server
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
