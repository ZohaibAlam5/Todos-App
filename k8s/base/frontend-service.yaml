apiVersion: v1
kind: Service
metadata:
  name: taskflow-frontend
  labels:
    app: taskflow
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: taskflow
    component: frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
